export async function createUser({ params }: { params: object }) {
	try {
	} catch (e) {
		console.error(e);
	}
}

export async function deleteUser({ id }: { id: number }) {
	try {
	} catch (e) {
		console.error(e);
	}
}

export async function getUser({ id }: { id: number }) {
	try {
	} catch (e) {
		console.error(e);
	}
}

export async function getUsers() {
	try {
	} catch (e) {
		console.error(e);
	}
}

export async function modifyUser({ id }: { id: number }) {
	try {
	} catch (e) {
		console.error(e);
	}
}
