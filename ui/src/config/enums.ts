export enum AnswerType {
	FREEFORM = 'freeform',
	STANDARD = 'standard',
}

export enum ChecklistMode {
	VIEW,
	EDIT,
}

export enum QuestionType {
	FREEFORM = 'freeform',
	MULTI = 'multi',
	SINGLE = 'single',
}
