export enum AnswerType {
	FREEFORM = 'freeform',
	STANDARD = 'standard',
}

export enum ChecklistMode {
	VIEW,
	EDIT,
}

export enum QuestionType {
	DROPDOWN = 'dropdown',
	FREEFORM = 'freeform',
	MULTI = 'multi',
	SINGLE = 'single',
}
